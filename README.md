# 🕸️ Web Scraper Project (Python)

A simple yet powerful web scraper built using **Python**, **Requests**, and **BeautifulSoup4**.  
It allows users to extract titles, headings, paragraphs, and links from any webpage and export them into **TXT or CSV** format.

---

## 🚀 Features
- Fetches and parses any given webpage URL.
- Lets users choose what to extract:
  - Page title
  - Headings (h1, h2, h3)
  - Paragraphs
  - Links
- Supports keyword filtering.
- Saves output in `.txt` or `.csv` files.

---

## 🧩 Tech Stack
- **Python 3**
- **Requests**
- **BeautifulSoup4**
- **CSV module**

---

## ⚙️ How to Run
1. Clone this repository or download the files.
2. Install required dependencies:
   ```bash
   pip install -r requirements.txt
